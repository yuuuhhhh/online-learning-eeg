# 原始采集改造验收 v2.2

- [x] 原始 33-byte BLE 包按接收顺序写入 `eeg_raw.bin`。
- [x] 原始 signed 24-bit ADC counts 逐实际接收包写入 `eeg.csv`。
- [x] 丢包不填充，重复包显式标记，重连创建新 `stream_segment`。
- [x] 权威 `events.csv` 有唯一事件 ID、主机时间、样本映射、对齐状态与来源客户端。
- [x] 浏览器重试通过 `client_event_id` 去重，关闭前确认队列落盘。
- [x] 采集状态只检查连接、数据流、丢包、重复包、断流、饱和和平线。
- [x] 基线不再建立 RMS 阈值；只有无数据阻止继续。
- [x] UI 不显示 RMS、高频污染、有效窗口或训练可用率。
- [x] 采集结束不导入/调用切窗或预处理代码。
- [x] 不生成 `windows.csv`、`probe_epochs.csv` 或预处理文件。
- [x] `session_raw.mat` 不含训练掩码、窗口或 epoch。
- [x] 关闭原始文件后计算 SHA-256，并在报告/MAT 前后复核未改变。
- [x] 报告或 MAT 失败不会删除原始 EEG、原始包或事件。
- [x] 自动测试覆盖原始保存、同步、不可变性和禁止输出。
