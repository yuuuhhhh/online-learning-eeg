# 修改清单逐项验收（更新至 2026-09-13）

本表以用户提供的《线上学习 EEG 系统 v1.3 适配 v2.1 方案修改需求清单》为检查基线；其中“采集时测量 50 Hz”、60 秒采前静息 QC 和 Block 3 后 180 秒中场休息均已被负责人后续指令明确覆盖，状态记为“按新指令替代”。

## P0

| 编号 | 状态 | 实现与证据 |
|---|---|---|
| P0-01 删除屏幕口算 | 完成 | 新页面无算式/F/J；采集端拒绝新 `math_*`/`arithmetic_*`，MAT 保留历史兼容。 |
| P0-02 连续减 7 练习 | 完成 | 统一说明、start/end/confirm 事件；未确认不能开始 30 秒 QC 或 block；练习 EEG 非正式。 |
| P0-03 B 起始数字 | 完成 | 会话创建时为 3 个 B block 预生成互异 800–1000 素数，保存生成/显示/隐藏时间。 |
| P0-04 A/B 文案与标签 | 完成 | A=`focused`，B=`bbbd_subtraction`；condition weak label 与 probe strong label 分离。 |
| P0-05 G01–G12 | 完成 | 配置保存完整表，锁定视频/条件/位置；按阶段循环分配，复测沿用首次组；`session_half` 替代旧组概念。 |
| P0-06 block/休息 | 完成；中场休息按新指令替代 | 6 block；Block 1–5 后均休息 30 秒；pilot/formal 不可提前继续，smoke 可快速验证。 |
| P0-07 probe 时间表 | 完成 | 每 block 4 个，45/30/45 秒硬约束，基于 video clock，播放前保存；短视频直接拒绝。 |
| P0-08 probe/confidence | 完成 | 四选项固定映射，1–4 confidence，事件链校验，10 秒 epoch 和恢复后 2 秒 transition mask。 |
| P0-09 self-caught | 完成（待 pilot 冻结键值） | 当前专用键 K；A/B 语义分别保存；±2 秒 motor mask、前 10 秒 sensitivity mask；不覆盖 probe。 |
| P0-10 完整问卷 | 完成 | A/B 4 个通用必填项；B 追加 3 项；逐项事件和独立 `block_ratings.csv`。 |
| P0-11 视频与课堂题 | 软件部分完成；材料待补 | 已接入 6 段新视频、校验实际时长；每段 4 道题，题库来自新 DOCX，不再硬编码 HTML。尚未收到 2–3 段候选备用视频，也未做人类 pilot 难度标定。 |
| P0-12 事件同步字段 | 完成 | 身份、G 组、block/视频/条件、浏览器/采集时间、设备样本均为一等列；覆盖清单所列事件。 |
| P0-13 双层标签 | 完成 | 原始 A/B weak label；probe 前 10 秒强标签；4 秒/2 秒窗；Dataset A/B、问卷协变量与训练 mask。 |
| P0-14 采前静息 QC | 完成；时长和 50 Hz 按新指令替代 | 固定睁眼 30 秒，检查数据持续、饱和、平线、DC/极端幅值、高频污染、丢包；override 必须实验员+原因。采集端不测 50 Hz。 |
| P0-15 2 秒 watchdog | 完成 | 超过约 2 秒无数据自动暂停，记录 dropout；恢复记录时长，实验员确认后才 resume。 |
| P0-16 窗口/会话 QC | 完成；50 Hz 按新指令替代 | `GOOD/USABLE/REJECT` 与多原因 mask；会话汇总丢包、最长缺口、掉线、饱和、RMS、per-block ratio。50 Hz 留给离线副本。 |
| P0-17 完整性检查 | 完成 | 结束生成规范化表、`session_qc_report.json` 和 MAT；缺 probe/问卷/题目/B 字段明确 FAIL。 |
| P0-18 配置与追溯 | 完成（formal 有意未解锁） | 未覆盖原目录；集中 JSON 配置；metadata 保存软件/协议/配置版本、SHA-256、阶段、G 组、材料和实际流程。未冻结配置拒绝 formal。 |

## P1

| 编号 | 状态 | 说明 |
|---|---|---|
| P1-01 状态机 | 基本完成 | 练习→QC→按序 block→休息/问卷/题目→完整性检查有门禁；重复 probe 与乱序 block 由采集端拒绝。紧急结束会在报告中暴露缺项。 |
| P1-02 恢复/崩溃保护 | 完成（安全降级） | 待发事件 localStorage 备份、run ID 核对、采集端状态回传、关闭握手和异常后 MAT 重建已实现。完整 block 之间刷新可在重新选择视频后继续；block/问卷/题目/休息中刷新因浏览器不能恢复本地视频句柄或精确状态，会自动锁定、写 `browser_recovery_blocked` 并让报告标 incomplete，禁止覆盖旧记录。 |
| P1-03 降误操作 | 完成 | 采集端与被试网页分区；会话后锁身份/G 组，block 后锁视频/条件；页面持续显示 G 组、block、视频、condition、EEG 状态。 |
| P1-04 规范化输出 | 完成 | 新增 `probes.csv`、`block_ratings.csv`、`quiz_responses.csv`、`probe_epochs.csv`、`windows.csv` 与数据字典。 |

## 用户本次 50 Hz 覆盖指令

- 采集端已删除 50 Hz FFT/功率比计算、阈值、页面显示、`qc.csv` 字段、窗口 `warning_50hz/needs_notch/line_noise` 逻辑。
- 原始数据不滤波。
- `preprocess_eeg.py` 对 `eeg.csv` 的副本按连续 stream segment 做 50 Hz 零相位陷波/带阻并记录报告。
- 使用“陷波/带阻”而非“带通”，因为后者会保留 50 Hz。

## 自动验收

- Python：22 项通过。
- 浏览器静态/语法/协议检查：22 项通过。
- 合成完整会话：`PASS`，实际计数为 6/24/6/24/3/3。
- 合成缺项会话：`FAIL`，能指出 probe 23/24、问卷 5/6、题目 23/24、B 专属评分 2/3。

## 进入下一阶段前仍需真实人员完成

1. 补充并登记 2–3 段候选备用视频。
2. 两名真实参与者完成 smoke test；记录设备、网页、理解度和输出文件问题。
3. 修复 smoke 阻断问题后进行 6–8 人 pilot。
4. 负责人依据 pilot 冻结 probe 数、K 键、mask、材料容差、QC 阈值和 override 处理规则。
5. 发布 v2.2 Final、更新配置版本/哈希并将 `frozen_for_formal=true`，之后才开始正式采集。
