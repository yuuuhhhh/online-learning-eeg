# 实验组使用说明（v2.2 原始采集）

本系统只采集原始 EEG 与实验标签，不在现场判断数据能否用于训练。

## 实验前与固定流程

1. 运行 `setup_environment.cmd`，再启动 `start_integrated_experiment.cmd`。
2. 填写匿名被试、会话、阶段和实验员编号；正式采集选择 `formal`，并使用系统推荐的 G 组。
3. 网页一次选择并匹配六段正式视频，完成连续减 7 练习确认。
4. 连接耳机并启动数据流，点击“开始基线采集”，记录固定 30 秒睁眼静息原始 EEG。
5. 基线中的饱和、平线或丢包只提示现场检查，不产生科学质量结论；只有没有持续收到 EEG 时才必须恢复数据流并重测。
6. 按锁定顺序运行 Block 1–6，完成 thought probe、问卷、减 7 报告和理解题。
7. 人工异常按钮只写入事件标记，不会删除附近样本或自动生成排除区间。
8. Block 6 后保持网页打开，关闭采集窗口，等待网页队列、CSV/BIN 刷盘、哈希、报告和可选 MAT 完成。

## EEG 中断

- 连续约 2 秒没有新 EEG 时，系统记录 `eeg_dropout` 并暂停视频。
- 恢复后记录 `eeg_recovery`，不会补齐断流期间的 EEG。
- 实验员确认同步后再继续视频；暂停、恢复和持续时长均写入事件/报告。
- 已经接收的数据在任何状态下都不得删除。

## 结束后检查

确认以下文件可读：`eeg.csv`、`eeg_raw.bin`、`events.csv`、`acquisition_qc.csv`、`probes.csv`、`block_ratings.csv`、`quiz_responses.csv`、`metadata.json`、`session_acquisition_report.json`、`checksums.sha256`，以及启用时的 `session_raw.mat`。

`session_acquisition_report.json` 的 PASS/WARN/FAIL 只反映采集和标签完整性，如样本/原始包数量、丢包、重复包、断流、Block/probe/问卷/理解题是否完整、事件是否对齐和原始文件哈希。它不包含有效窗口率、伪迹比例或训练可用性。

如原始文件写入失败，采集端会显示红色报警；不要继续实验，保留当前 run 并记录现场情况。离线处理必须从原始文件副本开始，且不得覆盖原始文件。
