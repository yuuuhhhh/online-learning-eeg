# 实验组使用说明（v2.1 pilot）

本版只能选择 `smoke` 或 `pilot`。在负责人完成 pilot 参数冻结前，选择 `formal` 会被程序拒绝。

## 一、实验前准备

1. 双击 `setup_environment.cmd` 完成首次安装。
2. 确认 `materials/videos/` 中六段 MP4 和 `config/protocol_v2.1.json` 都存在。
3. 双击 `start_integrated_experiment.cmd`，不要直接双击 HTML。
4. 输入匿名被试号、会话号、研究阶段和实验员号。一般采用系统推荐 G 组；同一被试复测必须沿用首次 G 组。
5. 网页点击“选择六个视频”，一次选择 `materials/videos/` 下全部 MP4。六个视频必须全部匹配、实际时长须与配置接近，并能容纳 4 个 probe。

## 二、固定流程

1. 被试阅读连续减 7 规则，从练习数字开始在脑内连续减 7，不出声、不用手指明显计数、不持续按键。实验员确认“已理解并能执行”。
2. 连接耳机并启动数据流。在采集窗口点击“开始采前 QC”，记录固定 60 秒睁眼、坐稳、尽量不动的静息 EEG。
3. QC 未通过时先重戴并检查参考地、电极接触、供电和充电线，再重测。确需放行时必须填写实验员编号和具体原因。
4. 网页按 G 组锁定的顺序运行 Block 1–6，不得手动更换视频或条件。
5. A：像正常线上学习一样观看并理解记忆。B：记住屏幕给出的素数；视频开始时数字消失，之后在脑内持续减 7。
6. 每个 block 自动出现 4 个 thought probe。视频会暂停；先答注意位置，再答 1–4 分 confidence，两问提交后视频恢复。
7. `K` 是 self-caught 专用键。A 中表示发现注意已离开课程并回到视频；B 中表示发现已停止减 7、转向课程，随后重新开始心算。不要用 `S` 代替，`S` 仅为人工同步标记。
8. 视频后填写全部问卷。A 填 4 项；B 还须填减 7 执行比例、减 7 难度和最终数字。随后完成恰好 4 道课堂题。
9. Block 1–5 后休息 30、30、180、30、30 秒。pilot 中倒计时结束前不能继续。
10. Block 6 后查看完整性预检查。保持网页打开，关闭采集窗口；等待事件队列清空、CSV/BIN 刷盘、报告和 MAT 生成后再关网页。

## 三、EEG 中断处理

- 连续约 2 秒无新 EEG 时，采集端记录 `eeg_dropout`，网页自动暂停并阻止 probe/新 block。
- 数据恢复后会记录 `eeg_recovery`，但视频不会自动继续。
- 实验员确认设备与视频同步无误后，点击“确认同步并继续”；系统记录 `eeg_recovery_confirmed` 和带 `pause_reason=eeg_dropout` 的 `video_resume`。
- 不要刷新页面解决掉线。完整 block 之间刷新后可重新选择六段视频继续；若在 block、问卷、题目或休息中刷新，网页会锁定并记录 `browser_recovery_blocked`，该 run 必须以 incomplete 保存，不能覆盖重跑。

## 四、结束后检查

打开 run 目录中的 `session_qc_report.json`：

- `PASS`：结构完整；仍需结合现场记录判断能否进入分析。
- `WARN`：结构完整但有人工 override、非正式阶段等需要复核事项。
- `FAIL`：明确缺失 block、probe、问卷、题目、B 数据、暂停配对或 EEG；不得作为完整会话。

同时确认 `eeg.csv`、`events.csv`、`eeg_raw.bin`、`qc.csv`、`probes.csv`、`block_ratings.csv`、`quiz_responses.csv`、`probe_epochs.csv`、`windows.csv`、`metadata.json` 和 `session.mat` 可读取。

采集阶段不检查 50 Hz，也不会显示 50 Hz 指标。需要去除工频时，在 run 目录上运行 `preprocess_eeg.py`，输出副本；不要覆盖或手工改写原始 EEG。

## 五、smoke 调试专用功能

只有 `smoke` 阶段显示“快速 probe”和“立即触发”控件；它们生成的会话不得进入 pilot 或正式分析。smoke 可提前结束休息，用于快速检查页面链路。pilot 阶段强制正式 probe 约束和完整休息。

## 六、常见阻断

- “未完成连续减 7 练习”：先在网页完成练习确认。
- “采前 QC 未通过”：重戴、排查、重测；人工 override 必须留名和原因。
- “must use Vx / 下一个必须是 Block x”：选择与 G 组计划不一致；不能绕过。
- “视频时长不足，无法容纳 4 个 probe”：更换合适视频，程序不会静默减少 probe。
- “等待 EEG 恢复”：确认数据重新到达后，再由实验员点击继续。
- 关闭窗口时提示浏览器事件尚未保存：保持网页和采集端打开，等待队列清空后重试关闭。

现场测试请使用 [docs/Smoke与Pilot执行单.md](./docs/Smoke与Pilot执行单.md)，不要把旧版文档当作新流程说明。
